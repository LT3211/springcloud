spring:
  application:
    name: configclient
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8762 #配置本地ConfigServer的访问路径
      fail-fast: true #设置客户端优先判断Config Server获取是否正常